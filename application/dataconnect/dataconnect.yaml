specVersion: "v1beta"
serviceId: "application"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "application-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
